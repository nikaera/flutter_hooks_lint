name: flutter_hooks_lint_flutter_test
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  custom_lint: ">=0.6.0"
  custom_lint_core: ">=0.6.0"
  hooks_riverpod: ^2.4.10
  test: ^1.24.0
  flutter_hooks_lint:
    path: ../flutter_hooks_lint
  flutter_test:
    sdk: flutter

resolution: workspace
